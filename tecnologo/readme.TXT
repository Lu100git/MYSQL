the following code creates a simple school database

with triggers that stores changes in tables, 

the records table has no relations because we only store changes in the databse

for tracking changes
